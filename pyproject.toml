[project]
name = "twin-speech"
version = "0.1.0"
description = "Post-meeting speech coach: speaker-attributed transcripts, coaching cards, and pronunciation analysis."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "modal>=1.3.5",
]
